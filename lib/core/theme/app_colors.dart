import 'package:flutter/material.dart';

abstract final class AppColors {
  // Surfaces
  static const background = Color(0xFFF0EDE6);
  static const surface = Color(0xFFFAF7F2);
  static const card = Color(0xFFFFFFFF);

  // Text
  static const textPrimary = Color(0xFF26221C);
  static const textSecondary = Color(0xFF6B645A);
  static const textMuted = Color(0xFF8A8175);
  static const textMutedAlt = Color(0xFF8A7F6B);

  // Brand — deep burgundy
  static const teal = Color(0xFF6E1E33);
  static const tealDark = Color(0xFF4A1524);
  static const tealBg = Color(0xFFF6E7EA);
  static const tealMuted = Color(0xFF8C4A57);

  // Gold accent (dividers, embroidery-style flourishes)
  static const gold = Color(0xFFC9A227);
  static const goldMuted = Color(0xFFB68A3E);

  // Sand / gold (Eid banner, tips)
  static const sandBg = Color(0xFFF3EAD9);
  static const sandText = Color(0xFF7A6A44);
  static const sandTextDark = Color(0xFF5C4A22);

  // Borders — blush pink
  static const border = Color(0xFFEAD6D9);
  static const borderAlt = Color(0xFFF0E0E3);
  static const borderStrong = Color(0xFFE0BFC5);

  // Occasion tile backgrounds
  static const occasionSand = Color(0xFFE9DFCD);
  static const occasionMint = Color(0xFFDFE9E7);
  static const occasionTaupe = Color(0xFFDED4C0);
  static const occasionCream = Color(0xFFEFE4D0);

  // Status
  static const error = Color(0xFFB3261E);
  static const chipUnselectedBorder = Color(0xFFC9C0B0);
}
